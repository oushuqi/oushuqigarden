---
{"dg-publish":true,"permalink":"/02-在梦境里遨游/日常/星星/","dgPassFrontmatter":true,"noteIcon":""}
---


<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>严格3x3九宫格图片</title>
<style>
  body {
    font-family: Arial, sans-serif;
    background-color: #f0f0f0;
    padding: 20px;
    display: flex;
    justify-content: center;
  }

  /* 九宫格 */
  .gallery {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-template-rows: repeat(3, 1fr);
    grid-gap: 5px;
    max-width: 600px;
    width: 100%;
  }

  .gallery img {
    width: 100%;
    aspect-ratio: 1 / 1; /* 保持正方形 */
    object-fit: cover;
    cursor: pointer;
    border-radius: 8px;
    transition: transform 0.3s;
  }

  .gallery img:hover {
    transform: scale(1.05);
  }

  /* 弹窗遮罩 */
  .modal {
    display: none;
    position: fixed;
    z-index: 1000;
    top: 0; left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.9);
    justify-content: center;
    align-items: center;
  }

  .modal.active {
    display: flex;
  }

  .modal-content {
    position: relative;
    max-width: 90%;
    max-height: 90%;
  }

  .modal-content img {
    width: 100%;
    height: auto;
    border-radius: 8px;
  }

  /* 左右箭头 */
  .arrow {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    font-size: 2rem;
    color: white;
    background: rgba(0,0,0,0.3);
    padding: 10px;
    border-radius: 50%;
    cursor: pointer;
    user-select: none;
  }

  .arrow-left { left: -50px; }
  .arrow-right { right: -50px; }

  @media (max-width: 600px) {
    .arrow-left { left: 10px; }
    .arrow-right { right: 10px; }
  }
</style>
</head>
<body>

<div class="gallery">
  <img src="https://picsum.photos/id/1015/300" alt="1">
  <img src="https://picsum.photos/id/1016/300" alt="2">
  <img src="https://picsum.photos/id/1018/300" alt="3">
  <img src="https://picsum.photos/id/1020/300" alt="4">
  <img src="https://picsum.photos/id/1024/300" alt="5">
  <img src="https://picsum.photos/id/1025/300" alt="6">
  <img src="https://picsum.photos/id/1033/300" alt="7">
  <img src="https://picsum.photos/id/1035/300" alt="8">
  <img src="https://picsum.photos/id/1036/300" alt="9">
</div>

<div class="modal" id="modal">
  <div class="modal-content">
    <span class="arrow arrow-left">&#10094;</span>
    <img src="" id="modal-img" alt="大图">
    <span class="arrow arrow-right">&#10095;</span>
  </div>
</div>

<script>
const galleryImages = document.querySelectorAll('.gallery img');
const modal = document.getElementById('modal');
const modalImg = document.getElementById('modal-img');
const arrowLeft = document.querySelector('.arrow-left');
const arrowRight = document.querySelector('.arrow-right');

let currentIndex = 0;

function showModal(index) {
  currentIndex = index;
  modalImg.src = galleryImages[index].src.replace('/300','/800'); // 大图
  modal.classList.add('active');
}

function closeModal() {
  modal.classList.remove('active');
}

galleryImages.forEach((img, index) => {
  img.addEventListener('click', () => showModal(index));
});

modal.addEventListener('click', (e) => {
  if (e.target === modal || e.target === modalImg) {
    closeModal();
  }
});

arrowLeft.addEventListener('click', (e) => {
  e.stopPropagation();
  currentIndex = (currentIndex - 1 + galleryImages.length) % galleryImages.length;
  modalImg.src = galleryImages[currentIndex].src.replace('/300','/800');
});

arrowRight.addEventListener('click', (e) => {
  e.stopPropagation();
  currentIndex = (currentIndex + 1) % galleryImages.length;
  modalImg.src = galleryImages[currentIndex].src.replace('/300','/800');
});

// 键盘控制
document.addEventListener('keydown', (e) => {
  if (!modal.classList.contains('active')) return;
  if (e.key === 'ArrowLeft') arrowLeft.click();
  if (e.key === 'ArrowRight') arrowRight.click();
  if (e.key === 'Escape') closeModal();
});
</script>

</body>
</html>
